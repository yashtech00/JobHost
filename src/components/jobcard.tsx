import { CardContent } from "./ui/card";


export function JobCard({job}){
    return <div>
        <CardContent>
            <div>{job.title}</div>
            <div>{job.title}</div>
            <div>{job.title}</div>
            <div>{job.title}</div>
            <div>{job.title}</div>
        </CardContent>
    </div>
}